// Minimal stub for EvaluationService used in serverless/script contexts.
// TODO: Replace with full implementation if evaluation pipeline is required outside UI.

export class EvaluationService {
  isAvailable(): boolean {
    return false;
  }
}
