# Hidden Features Log

We remove (or hide) UI surface areas here when the backend experience is not ready. Re-enable each item once the underlying functionality ships.

- **Job description URL ingestion button** – temporarily hidden in `CoverLetterCreateModal.tsx` (see `TODO` near the textarea). Reactivate after remote JD fetching is implemented.
